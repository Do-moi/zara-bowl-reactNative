import react from "react"
import Menu from './Menu';

export const HomeScreen = ({navigation}) => <Screen navigation={navigation} name="home"/>
export const SearchScreen = ({navigation}) => <Screen navigation={navigation} name="Search"/>
export const DetailScreen = ({navigation}) => <Screen navigation={navigation} name="Details"/>
